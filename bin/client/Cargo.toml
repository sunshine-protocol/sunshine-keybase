[package]
name = "test-client"
version = "0.1.0"
authors = ["David Craven <david@craven.ch>"]
edition = "2018"

[features]
mock = [
    "env_logger",
    "jsonrpsee",
    "sp-keyring",
    "tempdir",
    "test-node",
]

[dependencies]
client-faucet = { path = "../../faucet/client" }
client-identity = { path = "../../identity/client" }
substrate-subxt = "0.9.0"
utils-identity = { path = "../../identity/utils" }

env_logger = { version = "0.7.1", optional = true }
jsonrpsee = { version = "0.1.0", optional = true }
sp-keyring = { version = "2.0.0-rc3", optional = true }
tempdir = { version = "0.3.7", optional = true }
test-node = { path = "../node", optional = true }
